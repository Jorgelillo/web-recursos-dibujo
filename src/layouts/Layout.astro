---
import '../styles/global.css';  // <--- ¡AÑADE ESTA LÍNEA AQUÍ!
import Header from '../components/Header.astro'; // <--- IMPORTANTE

interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Recursos educativos de Dibujo y Plástica" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title} | Aula de Dibujo</title>
	</head>

	<body class="bg-gray-50 text-slate-800 min-h-screen flex flex-col">
		
		<Header class="bg-white shadow-sm sticky top-0 z-50">
			<nav class="container mx-auto p-4 flex justify-between items-center">
				<a href="/" class="text-xl font-bold text-indigo-600 hover:text-indigo-800">
					🎨 Aula de Dibujo
				</a>
				<div class="space-x-4">
					<a href="/" class="hover:text-indigo-600 transition">Inicio</a>
					</div>
			</nav>
		</Header>

		<main class="flex-grow">
			<slot />
		</main>

		<footer class="bg-slate-800 text-white py-8 mt-12">
			<div class="container mx-auto text-center px-4">
				<p class="text-sm opacity-70">
					&copy; {new Date().getFullYear()} Recursos Educativos de Dibujo. 
					Compartiendo conocimiento.
				</p>
			</div>
		</footer>

	</body>
</html>