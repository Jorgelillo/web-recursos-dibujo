---
// src/components/Header.astro
import { getCollection } from 'astro:content';

// 1. Obtenemos las asignaturas para el menú desplegable
const asignaturas = await getCollection('asignaturas');
// Ordenamos alfabéticamente
asignaturas.sort((a, b) => a.data.titulo.localeCompare(b.data.titulo));
---

<header class="sticky top-0 z-50 w-full bg-white/80 backdrop-blur-md border-b border-gray-100 shadow-sm transition-all">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      
      <div class="flex-shrink-0">
        <a href="/" class="flex items-center gap-2 group">
          <span class="text-2xl transition-transform group-hover:scale-110">🎨</span>
          <span class="font-bold text-xl tracking-tight text-gray-900 group-hover:text-indigo-600 transition-colors">
            Plástica<span class="text-indigo-600">Web</span>
          </span>
        </a>
      </div>

      <nav class="hidden md:flex items-center space-x-1 lg:space-x-2">
        <a href="/" class="px-3 py-2 rounded-lg text-sm font-medium text-gray-600 hover:text-indigo-600 hover:bg-indigo-50 transition-all">
          Inicio
        </a>
        
        <div class="relative group py-4">
          <button class="flex items-center gap-1 px-3 py-2 rounded-lg text-sm font-medium text-gray-600 hover:text-indigo-600 hover:bg-indigo-50 transition-all">
            Asignaturas
            <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 transition-transform group-hover:rotate-180" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          
          <div class="absolute top-full left-1/2 -translate-x-1/2 w-64 bg-white shadow-xl rounded-xl border border-gray-100 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 transform translate-y-2 group-hover:translate-y-0 overflow-hidden">
            <div class="p-2 grid gap-1">
              {asignaturas.map((asig) => (
                <a 
                  href={`/asignaturas/${asig.slug}`} 
                  class="block px-4 py-3 rounded-lg hover:bg-indigo-50 transition"
                >
                  <p class="text-sm font-semibold text-gray-800">{asig.data.titulo}</p>
                  <p class="text-xs text-gray-500">{asig.data.curso}</p>
                </a>
              ))}
            </div>
          </div>
        </div>

        <a href="/calendario" class="px-3 py-2 rounded-lg text-sm font-medium text-gray-600 hover:text-orange-600 hover:bg-orange-50 transition-all">
          📅 Calendario
        </a>
        <a href="/artistas" class="px-3 py-2 rounded-lg text-sm font-medium text-gray-600 hover:text-purple-600 hover:bg-purple-50 transition-all">
          👩‍🎨 Artistas
        </a>
        <a href="/sobre-mi" class="px-3 py-2 rounded-lg text-sm font-medium text-gray-600 hover:text-indigo-600 hover:bg-indigo-50 transition-all">
          Sobre mí
        </a>
        
        <a href="/buscar" class="ml-2 px-4 py-2 rounded-full text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 shadow-sm hover:shadow-md transition-all flex items-center gap-2 transform hover:-translate-y-0.5">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
          </svg>
          Buscar
        </a>
      </nav>

      <div class="md:hidden flex items-center">
        <button id="mobile-menu-btn" class="p-2 rounded-lg text-gray-600 hover:text-indigo-600 hover:bg-indigo-50 transition-colors" aria-label="Abrir menú">
          <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      </div>
      
    </div>
  </div>

  <div id="mobile-menu" class="hidden md:hidden border-t border-gray-100 bg-white/95 backdrop-blur-md absolute w-full shadow-xl">
    <div class="px-4 pt-2 pb-6 space-y-1 max-h-[80vh] overflow-y-auto">
      
      <a href="/" class="block px-3 py-3 rounded-lg text-base font-medium text-gray-700 hover:text-indigo-600 hover:bg-indigo-50 transition-colors">
        Inicio
      </a>
      
      <div class="px-3 py-2 mt-2">
        <p class="text-xs font-bold text-gray-400 uppercase tracking-wider mb-2">Asignaturas</p>
        <div class="pl-3 border-l-2 border-indigo-100 space-y-1">
          {asignaturas.map((asig) => (
            <a href={`/asignaturas/${asig.slug}`} class="block py-2 text-sm text-gray-600 hover:text-indigo-600 transition-colors">
              {asig.data.titulo}
            </a>
          ))}
        </div>
      </div>

      <a href="/calendario" class="block px-3 py-3 rounded-lg text-base font-medium text-gray-700 hover:text-orange-600 hover:bg-orange-50 transition-colors">
        📅 Calendario Escolar
      </a>
      <a href="/artistas" class="block px-3 py-3 rounded-lg text-base font-medium text-gray-700 hover:text-purple-600 hover:bg-purple-50 transition-colors">
        👩‍🎨 Artistas
      </a>
      <a href="/sobre-mi" class="block px-3 py-3 rounded-lg text-base font-medium text-gray-700 hover:text-indigo-600 hover:bg-indigo-50 transition-colors">
        Sobre mí
      </a>
      
      <a href="/buscar" class="block mt-6 px-3 py-3 rounded-lg text-base font-bold text-indigo-700 bg-indigo-50 hover:bg-indigo-100 transition-colors flex items-center justify-center gap-2 border border-indigo-100">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
        </svg>
        Buscar tareas
      </a>

    </div>
  </div>
</header>

<script>
  // Script para hacer funcionar el botón del móvil
  const btn = document.getElementById('mobile-menu-btn');
  const menu = document.getElementById('mobile-menu');

  if (btn && menu) {
    btn.addEventListener('click', () => {
      menu.classList.toggle('hidden');
    });
  }
</script>